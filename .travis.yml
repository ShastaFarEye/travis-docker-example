sudo: required

language: python

services:
  - docker

before_install:
- docker pull mazaclub/encompass-release:0.6.0
- docker pull mazaclub/encompass-winbuild:0.6.0
- cd ${TRAVIS_BUIL_DIR}
- cd contrib/mazaclub-release
- ./buildLinux 0.6.0 local

script:
- cd ${TRAVIS_BUILD_DIR}
- echo "Tanks" 
